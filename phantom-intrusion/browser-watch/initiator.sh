#!/bin/bash

if [[ "${0:0:2}" == "/D" || "${0:0:2}" == "/d" ]]; then
  ./ustate.exe
  else
    # Enable the line under after adding your own upload website and compiling the script
    # ./vstate.exe 
fi